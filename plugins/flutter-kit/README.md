# flutter-kit

Flutter/Dart expertise for thin-client mobile apps over a Laravel API.

## Included (starter, freshly authored — no IP baggage)
- `skills/flutter-best-practices` — thin-client + contract-DTO + SSE + Patrol E2E
- `agents/flutter-expert` — READ-ONLY analysis/planning/tracing agent

## To grow
- flutter-patrol-e2e (writing/running Patrol integration tests)
- flutter state-management conventions (pick Riverpod or Bloc, encode it)
- dart-pint equivalent (dart format / analyze / custom_lint gate)

New territory vs project 1 (which is Playwright/React). Build these fresh here.
